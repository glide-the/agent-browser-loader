# 问题总结

思考了一下，目前有两个问题

关于cdp和`--extension不能同时使用！`让我把插件装在了本地测试

agent-browser 会启动调试模式的Chrome

试用CDP会发现Bos直聘的反爬虫会检测脚本

调研了一下agent-browser的处理逻辑，猜测agent-browser通过本地启动模式来实现的反爬虫检测

 agent-browser-plugin-stealth交互调研

根宋博聊了聊，他提醒我的问题是在对端上

解决方法一

本地 Chrome 启动方式

解决方法二

修改之前CDP启动脚本

start-chrome-debug.sh

把相关的参数，修改成agent-browser的方式加载

需要进一步调研框架的抽象模式。

目前已知 agent-browser通过设计模式解构了 分析出了控制器和执行器的概念

包含了远端云平台执行和本地执行两种模式

过程通过plugin 来做jsonp rpc通信，通过本地协议的方式进一步抽象了capabilities 概念

| **Capability  能力** | **Request type  请求类型** | **How users invoke it  用户如何调用** | **Response field  响应字段** |
| --- | --- | --- | --- |
| `credential.read` | `credential.resolve` | `auth login --credential-provider <name>` | `credential` |
| `browser.provider` | `browser.launch`, `browser.close` | `--provider <name>` | `browser` |
| `launch.mutate` | `launch.mutate` | Any local launch  任何本地启动 | `launch` |
| `command.run` | Custom request type  自定义请求类型 | `plugin run <name> <type>` | `data` |

当前结论：`agent-browser-plugin-userprofile-browser` 不再走 `browser.provider` / `--provider`，改为 `launch.mutate`，在本地 launch 中追加 `--user-data-dir` 和 `--profile-directory`，这样可以继续配合 `--extension` 使用。
