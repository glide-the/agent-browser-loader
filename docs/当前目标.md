
我需要参考 https://agent-browser.dev/plugins#minimal-plugin 例子，来实现

两个功能现已合并进单个 launch.mutate 插件 `agent-browser-plugin-stealth`

launch.mutate ，在本地 launch 前注入 Profile 参数。
返回 `launch.args`，包含 `--user-data-dir`、`--profile-directory`，从而可以继续使用 `--extension`。

launch.mutate用于实现stealth的能力，流程为
插件进程 agent-browser-plugin-stealth 被启动
接收stdin 发送 JSON 请求
插件返回 args / extensions / initScripts / userAgent
